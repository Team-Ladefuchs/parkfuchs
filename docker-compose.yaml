version: "3.7"

services:
    pocketbase:
        image: spectado/pocketbase:latest
        restart: unless-stopped
        ports:
            - 8090:80
        volumes:
            - ./pocketbase-data:/pb_data
